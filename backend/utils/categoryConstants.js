// Suggested default categories for the frontend dropdown.
// Not enforced by the DB schema (category is free text) so users/frontend can add custom ones.
const DEFAULT_CATEGORIES = [
  'Food',
  'Transport',
  'Shopping',
  'Bills',
  'Entertainment',
  'Health',
  'Education',
  'Other',
];

module.exports = { DEFAULT_CATEGORIES };
